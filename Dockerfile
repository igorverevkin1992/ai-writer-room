# Используем легкий образ Node.js
FROM node:20-alpine

# Создаем рабочую папку внутри контейнера
WORKDIR /app

# Копируем файлы зависимостей
COPY package*.json ./

# Устанавливаем зависимости
RUN npm install

# Копируем исходный код проекта
COPY . .

# Открываем порт, на котором работает Vite
EXPOSE 5173

# Запускаем проект с флагом --host, чтобы он был доступен снаружи контейнера
CMD ["npm", "run", "dev", "--", "--host"]